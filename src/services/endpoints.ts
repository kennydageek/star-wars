const baseUrl = 'https://swapi.dev/api';

export const FETCH_FILMS = `${baseUrl}/films`;
export const FETCH_STARSHIPS = `${baseUrl}/starships`;
export const FETCH_PEOPLE = `${baseUrl}/people`;
export const FETCH_SPECIES = `${baseUrl}/species`;
